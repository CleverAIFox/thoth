.venv/
__pycache__/
*.pyc
.env
.terraform/
*.tfstate
*.tfstate.*
# ★ plan -out 산출물. 변수 값(토큰 포함)이 평문으로 들어간다(DECISIONS §115)
tfplan
*.tfplan
*.tfvars
!*.tfvars.example
dist/
*.zip
.cache/
node_modules/

infra/backend.hcl
# 기획서 생성기의 중간 산출물(DECISIONS §116). docx 만 커밋한다
docs/proposal/.build/
